import org.jax.mvarcore.Variant

model {
	Variant variant
}

json {
	variant variant.variantRefTxt
	ref variant.ref
	alt variant.alt
	gene variant.gene.symbol
	type variant.type
	strains tmpl.variantStrain(variant.variantStrains ?: [])
}
